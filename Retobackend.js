// reto 1: suma de pares
// si se mete un 4 debe dar 6

const num = 4
const sumpares = () => {
  let arr = []
  let i = 1
  if (i <= num) {
    if (i % 2 == 0)
      arr.push(i)
  i += 1
  console.log(arr)
} console.log(arr)
}
sumpares()

// reto 2: contar vocales (a, e, i, o, u)